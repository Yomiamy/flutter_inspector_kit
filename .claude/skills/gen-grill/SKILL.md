---
name: gen-grill
description: 在 planner 產出規格之前盤問需求，直到問題定義、觸發場景、成功標準、範圍邊界、既有覆蓋五項齊備才放行。需求已有文件背書或屬機械性改動時直接放行。觸發條件：gen-dev-workflow STAGE 0a 啟動時自動執行、或使用者說「盤問我」「grill」「/gen-grill」。
---

# 需求盤問閘門 (gen-grill)

在最強推論（planner / opus / xhigh）花成本寫出整份規格**之前**，先確認需求本身沒歪。

方向錯的規格，代價不只是重寫一份文件——STAGE 0b 的計畫、STAGE 2 的實作全部建在上面。愈晚發現愈貴。

## 🔴 本 skill 不自帶問法

盤問沿用 `brainstorming` 已驗證的提問紀律（一次只問一個、偏好選擇題、聚焦 purpose / constraints / success criteria）。

本 skill 的職責只有三件事：

1. **判定**需求是否收斂（對照下方五項判準）
2. **盤問**缺漏的那幾項，直到補齊
3. **產出** brief 交給 planner

### 與 `brainstorming` 的邊界

**本 skill 不呼叫 `brainstorming`。** 該 skill 的 checklist 是 `MUST` 且有序的——提問只是其第 2 步，後續強制「提出 2-3 個方案 → 展示設計取得批准 → 寫設計文件 → 呼叫 `writing-plans`」，其明訂終態是 `writing-plans`（`brainstorming/SKILL.md:53`），不會回到呼叫端。

整套跑下來會在 STAGE 0a 之前就產生設計文件與實作計畫，與 STAGE 0a 的 `docs/features/` 和 STAGE 0b 的 `docs/plans/` 重複，且流程會停在 `writing-plans` 而不是回來產 brief。

所以兩者的分工是：

| | 職責 | 終態 |
|:---|:---|:---|
| **`gen-grill`**（本 skill） | 只問缺漏項，補齊即停 | 回傳 brief 給 planner |
| **`brainstorming`** | 完整設計流程（提問 → 方案 → 設計 → 文件） | `writing-plans` |

本流程的設計與計畫由 STAGE 0a/0b 的 planner 負責，**盤問階段不產生任何文件、不提方案、不求批准**。

## 收斂判準（五項）

逐項對照。判準的形狀是「能指著說**第 N 項沒答**」，不是「感覺夠不夠清楚」。

| # | 項目 | 算齊備 | 不算齊備 |
|:-:|:---|:---|:---|
| **Q1** | 問題定義 | 說得出**要解決的具體問題**：誰在什麼情況下卡住、現況會怎樣 | 只說「想要 X 功能」——那是解法不是問題 |
| **Q2** | 觸發場景 | 說得出**至少一個具體場景**：什麼人、什麼操作、什麼前提下會遇到 | 「使用者可能會需要」這類假想需求 |
| **Q3** | 成功標準 | 說得出**可驗證的完成樣子**：做完後拿什麼判斷對不對 | 「更好用」「更清楚」這類無法證偽的敘述 |
| **Q4** | 範圍邊界 | 說得出**明確不做什麼**，尤其是相鄰、容易順手做掉的部分 | 完全沒有邊界，或只說「先做基本的」 |
| **Q5** | 既有覆蓋 | **已實查 codebase**：確認要做的東西不存在、或存在但不符需求，並附證據（符號 grep 結果 / 檔案路徑 / 行號） | 只憑文件敘述或印象判斷「這個還沒做」 |

**任一項缺 → 針對該項提問（一次一個，優先選擇題）→ 答完重新判定。**
五項齊備 → 放行，產出 brief。

### 為什麼有 Q5（本 repo 專屬，上游沒有）

因為本 repo 最大的 misalignment 來源不是「需求沒講清楚」，是**文件與實況漂移**：

- **§D4**（Issue #159）：照文件字面做完、610 tests 全綠，才發現落點錯一層且漏了核心需求，**整案棄發 PR**
- **本 skill 自己**（Issue #166）：文件稱 `brainstorming` 缺一問一答，實查證偽——機制早已存在
- **§P8 / §D6 / §P19**：三次標為待辦、實查已完成

Q5 花的是幾次 grep 的時間，擋的是整輪白工。

## 短路條件（直接放行，不盤問）

符合任一項即放行——盤問本身有成本，對已經清楚的需求施加摩擦會重演「人在迴路頻率過高」的老問題。

- **已有文件背書**：`docs/features/` 或 `docs/plans/` 已有對應規格或計畫
- **已 triage 的 issue**：需求帶 `#編號`，且 issue body 已含驗收條件
- **機械性改動**：改版號、修 typo、照既有表格逐項執行等，做法本身無歧義
- **quick 模式**：見下

> 短路只跳過 Q1–Q4。**Q5 一律要跑**——前三次漂移全發生在「看起來已有文件背書」的項目上，正是短路最容易放過的那一類。

## quick 模式行為

`quick` 是小修正通道，全套盤問與它的定位衝突。

**quick 模式下只跑 Q5（既有覆蓋實查），Q1–Q4 略過。**

理由：quick 處理的是描述已足夠具體的小改動（Q1–Q4 天然齊備），但「這個是不是已經做過了」跟改動大小無關——`§P8` 當初就是個 trivial 項目，實查才發現早已完成。

## 產出：brief 交接格式

放行時產出下列結構，交給 planner 作為 STAGE 0a 的輸入：

```markdown
## 需求 brief（gen-grill 產出）

**Q1 問題定義**：<要解決的具體問題>
**Q2 觸發場景**：<誰、什麼操作、什麼前提>
**Q3 成功標準**：<可驗證的完成樣子>
**Q4 範圍邊界**：<明確不做什麼>
**Q5 既有覆蓋實查**：
  - 查了什麼：<符號 / 檔案 / 文件>
  - 結果：<未實作 / 部分完成，缺 X / 已完成但不符需求，因為 Y>
  - 證據：<grep 結果、file:line>

**放行方式**：五項齊備 ｜ 短路（<哪一條>）｜ quick 模式
**盤問輪數**：<N>（短路時為 0）
```

planner 依此 brief 撰寫規格，不需再重新釐清需求。

**若 Q5 的實查結果與原始需求敘述衝突**（例如「文件說沒做，實查發現已完成」），**先回報使用者再繼續**——這種情況下需求本身可能要重新定義，不是往下寫規格的時機。

## 完成條件

五項齊備（或符合短路條件且 Q5 已跑），brief 已產出並交給 planner。
