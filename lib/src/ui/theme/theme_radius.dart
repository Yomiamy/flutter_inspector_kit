/// Border-radius tokens (shared corner scale).
///
/// Naming convention: the numeric value goes straight into the name
/// (`radius8`).
class ThemeRadius {
  static const double radius4 = 4.0;
  static const double radius8 = 8.0;
}
