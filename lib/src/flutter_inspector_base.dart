/// Core entry point for the flutter_inspector package.
///
/// This is a placeholder skeleton. Concrete inspector integrations will be
/// added in subsequent releases. The class currently exposes only package
/// metadata so that downstream code can depend on a stable public surface.
class FlutterInspector {
  /// Creates a [FlutterInspector] instance.
  const FlutterInspector();

  /// The current package version, mirroring `pubspec.yaml`.
  static const String version = '0.0.1';
}
