# §W2 重新定性：委派邊界 Sensor 的文件回寫

> **性質**：純文件修正，零程式碼變更。
> **來源**：`docs/brainstorm/2026-09-13-workflow-brainstorm.md` 的 §W2 章節
> （以標題錨點定位，不記行號——該節本次改寫後行數已變動）
> **實查日期**：2026-08-30

## 1. 問題

§W2 被標為 `⬜ 待辦`，但其「現況」描述與 `.claude/hooks/wf-guard-delegate-cwd.sh`
（390 行）的實際程式碼嚴重脫節。依文件現況動工，會去解一個大部分已經解掉的問題。

## 2. 實查證據

### 2.1 文件宣稱 vs 實況

| 文件宣稱 | 實查結果 | 判定 |
|:---|:---|:---|
| pre 端「檢查的是**字串**，不是實際能寫到哪」 | `run_pre()` 比對目標 worktree 絕對路徑，不符即 `sys.exit(2)`，**具實際阻擋力**（BUG-1 的 exit code 教訓已內化，程式碼內有註解說明） | ❌ 不成立 |
| 限制手段只是「prompt 裡的一段文字……沒有強制力」 | hook 在 `PreToolUse` 攔截，與 prompt 文字無關；另有 P-1～P-6 六道 false-positive 防護（payload 解析、工具名、worktree 判定、state 檔、branch 比對、stage 比對） | ❌ 不成立 |
| post 端「只告警不阻斷」是缺口 | 屬實，但為**刻意設計**（程式碼標記 `U-1`）：PostToolUse 時動作已完成，回捲無意義 | ⚠️ 屬實但非疏漏 |
| 「只涵蓋主 repo 與已知 worktree，寫到非 git 位置完全偵測不到」 | 屬實，但**非唯一缺口**。`take_status()` 用 `git status --porcelain --untracked-files=normal`（**未帶 `--ignored`**），故 `.gitignore` 忽略的檔案同樣不進快照與 `diff_entries()`；另有短暫寫入與巢狀 repo／submodule 內部亦不可見。四者同源，皆為以 git status 差集為觀測基底的固有天花板 | ✅ 成立（**主要缺口之一**） |

補充實況（文件未記載）：

- `whitelist_roots()` **優先動態解析**（git 內部目錄、`PUB_CACHE`、Flutter/Dart SDK 根由 `which` 反解相容 fvm、`TMPDIR`），**並保留 `~/.pub-cache`／`~/fvm`／`/tmp` 等 fallback 兜底**；設計原則見 `docs/features/2026-08-19-delegation-cwd-sensor.md:114`（以解析出的實際路徑為準，不寫死假設路徑），非「完全不存在硬編碼」
- `is_allowed()` 兩側都 `realpath`，防 symlink 與 `..` 繞過，並以路徑分隔符為界避免 `/x/.pub-cache-evil` 誤判命中
- `diff_entries()` 只回報 after 新增項，委派前既有的 dirty 檔不算越界（P-9）
- fail-open 設計（P-11）：hook 自身壞掉不連帶卡死 workflow
- 已有 `.claude/hooks/tests/test_delegate_cwd_logic.py`，執行結果 `OK`

### 2.2 五個候選方案的實查判定

| 候選方案 | 判定 | 依據 |
|:---|:---|:---|
| pre 端對正規化絕對路徑做強制校驗 | ✅ **已實作** | `is_allowed()` 雙側 `os.path.realpath` 比對 |
| 容器隔離 | ❌ 出局（**成本**，非能力不足） | 技術上做得到 fs 邊界，但對單人 Flutter package 過度工程——brainstorm 自己已如此評估過 Dagger／Container Use |
| 專用受限使用者 + 檔案系統權限 | ❌ 出局（**成本**，非能力不足） | 技術上做得到，但需改動系統層帳號，遠超問題嚴重度 |
| 受限掛載點 | ❌ 出局（**成本**，非能力不足） | 同上 |
| 限制寫入型委派，只允許唯讀分析 | ⏸ 保留 | 唯一完全落在專案內且確定有效，但會實質閹割 STAGE 2 委派能力 |

額外發現（文件未列）：`/usr/bin/sandbox-exec` 存在，是**目前環境可見、且不需改動本 repo 以外
系統層設定**的 fs 層限制候選之一。**上表三個 ❌ 項（容器、受限使用者、受限掛載點）技術上
同樣做得到 fs 邊界，出局理由是採用成本而非能力不足**——兩者是不同軸線，勿混為一談。
惟 sandbox-exec 亦有其阻礙：MCP server 於 `~/.claude.json` 以 `npx -y gemini-mcp-tool`
啟動、`"env": {}`——**那是全域設定，不在本 repo 內**，且會影響所有專案而非僅此 repo。

### 2.3 實用性判準（Linus 第三條）

| 指標 | 實測值 |
|:---|:---|
| `mcp__gemini-cli__ask-gemini` 累計呼叫 | **2 次**（`~/.claude.json` skillUsage） |
| `gemini` CLI | **未安裝**（`which gemini` → not found；僅有 `agy`） |
| `cwd-violations.log` | **從未產生**（全 repo find 零命中） |

→ 此委派路徑在本專案的實際使用量趨近於零。為零使用量的路徑補強安全邊界，
複雜度與嚴重度不匹配。

## 3. 變更範圍

**唯一檔案**：`docs/brainstorm/2026-09-13-workflow-brainstorm.md`

### Task 1 · 改寫 §W2 本體（以標題錨點定位）

- 標題狀態：`⬜ 待辦` → `⏸ 降級待命（2026-08-30 實查重新定性）`
- 現況表依 §2.1 重寫，pre／post 兩列改為實況並標注刻意設計
- 候選方案表依 §2.2 重寫，五項各帶出局／保留理由，補上 `sandbox-exec` 與其邊界限制
- 新增「實用性判準」小節，載入 §2.3 三項實測值
- 新增「重啟條件」三項：委派使用量顯著上升、MCP server 設定移入專案內、
  出現實際的越界事故
- 保留原「刻意不做」段落（不在 prompt 加無強制力文字、不改三條委派紀律措辭）

### Task 2 · 同步交叉指涉

- L1809 頭註：`§W2 仍未動` → 反映降級定性
- L1899 的 `settings.local.json:250,270` → 更正為 `:252,272`（實查行號）
- L1534 順位 6 的「R5 的擴大範圍未做」註記：補一句指向 §W2 已重新定性

## 4. 驗收條件

1. §W2 每項現況描述都對得上 `wf-guard-delegate-cwd.sh` 實際程式碼
2. 五個候選方案各有明確出局／保留理由，無懸空項
3. 實用性判準的三項數值可被重新實查驗證
4. **零程式碼變更**：`.claude/hooks/`、`.claude/settings.local.json` 不動
5. 不改三條委派紀律措辭（文件自己已明令「刻意不做」）
6. §W2 未被刪除——降級 ≠ 否決，重啟條件明確可查

## 5. 明確不做

- 不寫任何程式碼、不改 hook
- 不碰 `~/.claude.json`（專案邊界外，且影響所有專案）
- 不實作「唯讀委派」限制（保留為候選，非本次範圍）
- 不把 §W2 移進「不排程」表——它有明確重啟條件，與已否決項性質不同
